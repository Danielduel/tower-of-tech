import { VisualNovelContainer } from "@/apps/editor/components/containers/VisualNovelBox.tsx";
import { links } from "@/apps/editor/routing.config.ts";

export const More = () => {
  return (
    <VisualNovelContainer>
      Welcome to the Tower of Tech, the Beat Saber project that sorts technical
      maps. I have a few playlists for you, they vary in pacing, complexity and
      density.
    </VisualNovelContainer>
  );
};
